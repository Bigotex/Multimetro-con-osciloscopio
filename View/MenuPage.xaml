<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Multimetro1_0_2.View.MenuPage"
             xmlns:ViewModels="clr-namespace:Multimetro1_0_2.View_Model;assembly=Multimetro1_0_2"
             xmlns:local="clr-namespace:Multimetro1_0_2;assembly=Multimetro1_0_2">

    <ContentPage.Resources>
        <ResourceDictionary>

        </ResourceDictionary>
    </ContentPage.Resources>
    <StackLayout  Margin="15,0,15,45" Padding="0,0,0,0" HorizontalOptions="Start" VerticalOptions="Center">
        <ScrollView Orientation="Vertical" VerticalScrollBarVisibility="Always" >
            <StackLayout Margin="0,0,0,0">
                <Label Text="Herramientas de medición" HorizontalOptions="Center" FontSize="Large"></Label>
                <Button Clicked="OscBtn_Clicked" x:Name="oscBtn" Text="Osciloscopio" >

                </Button>
                <Button x:Name="ohmBtn" Text="ohmnímetro">

                </Button>
                <Button  x:Name="voltBtn" Text="Voltímetro"  >
                </Button>
                <Button  x:Name="ampBtn" Text="Amperímetro" >

                </Button>
                <Button Clicked="SerialBtn_Clicked" x:Name="serialBtn" Text="Monitor Serial" >

                </Button>


            </StackLayout>
        </ScrollView>
    </StackLayout>


</ContentPage>
